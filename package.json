{
  "name": "markspace",
  "version": "1.0.0",
  "private": true,
  "description": "Markspace - Zero-Trust, Privacy-First, Edge-Native Markdown Workspace",
  "license": "AGPL-3.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:api": "wrangler dev",
    "dev:ui": "npm run dev --workspace=apps/ui",
    "build": "npm run build:with-wasm",
    "ui:build": "npm run build:ui",
    "build:ui": "npm run build --workspace=apps/ui",
    "build:pages": "npm run ui:build && npx esbuild apps/api/src/index.ts --bundle --outfile=apps/ui/dist/_worker.js --platform=neutral --main-fields=module,main --format=esm --minify --external:cloudflare:* --external:node:* && node -e \"const fs=require('fs'); fs.cpSync('apps/ui/dist', 'static', {recursive: true});\"",
    "deploy:pages": "npm run build:wasm && npm run build:pages && npm run d1:migrate:prod && wrangler pages deploy static --project-name=markspace --no-bundle",
    "build:wasm": "tsx scripts/build-wasm.ts",
    "build:with-no-wasm": "npm run build:ui",
    "deploy:with-no-wasm": "npm run build:with-no-wasm && npm run d1:migrate:prod && wrangler deploy",
    "build:with-wasm": "npm run build:wasm && npm run build:ui",
    "deploy:with-wasm": "npm run build:with-wasm && npm run d1:migrate:prod && wrangler deploy",
    "typecheck": "tsc --noEmit && npm run typecheck --workspaces --if-present",
    "deploy": "npm run deploy:with-wasm",
    "d1:create": "wrangler d1 create markspace-db --binding DB && wrangler d1 migrations apply markspace-db --remote",
    "r2:create": "wrangler r2 bucket create markspace-bucket",
    "d1:migrate:local": "echo \"y\" | wrangler d1 migrations apply markspace-db --local",
    "d1:migrate:prod": "echo \"y\" | wrangler d1 migrations apply markspace-db --remote",
    "d1:execute:local": "echo \"y\" | wrangler d1 execute markspace-db --local",
    "d1:execute:prod": "echo \"y\" | wrangler d1 execute markspace-db --remote"
  },
  "devDependencies": {
    "@types/node": "^22.13.5",
    "tsx": "^4.19.3",
    "typescript": "^7.0.2",
    "wrangler": "^4.131.0"
  }
}
